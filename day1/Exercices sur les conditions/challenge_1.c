#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{

    int a;
    printf("entre un nombree :");
    scanf("%d",&a);
    if (a%2==0)
        printf("le nombre est pair !!!");
    else
        printf("le nombre est impair !!!");
    return 0;

}
